[package]
name = "exercises_downloader"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version="0.12.23" , features = ["blocking", "json"]}
serde = { version = "1.0.225", features = ["derive"] }
